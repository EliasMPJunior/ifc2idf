services:
    bim2idf:
        image: eliasmpjunior/bim2idf:latest
        platform: linux/amd64
        build:
            context: ../../
            dockerfile: ./docker/config/Dockerfile
        container_name: bim2idf
        hostname: bim2idf
        volumes:
            - ../../src:/app
            - ../../workbench:/data
            - ../../bim2sim:/opt/conda/lib/python3.10/site-packages/bim2sim
        ports:
            - "${PORT:-8081}:8000"
        environment:
            - TZ=America/Sao_Paulo
        command:
            - bash
            - -c
            - |
                tail -f /dev/null